name: load_raster
description: "Load a raster layer from a local path."
category: data
terminal: false

display:
  title: "Load raster layer"
  command: "load_raster {input_path}"

required_slots: [input_path]
properties:
  input_path:
    type: string
    description: "Local raster dataset path."
  name:
    type: string
    description: "Optional layer alias."

groups: [group:data]
tags: []
